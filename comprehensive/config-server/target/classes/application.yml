server:
  port: 9090

spring:
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/zhudeming/spring-cloud-config.git
          search-paths: SC-BOOK-CONFIG
  application:
    name: config-server