spring:
  cloud:
    config:
      label: master
      uri: http://localhost:9090
      name: config-info
      profile: dev
      backup:
        enable: true
        fallbackLocation: E:\\study\\springcloud\\config-client-ha\\config\\fallback.properties