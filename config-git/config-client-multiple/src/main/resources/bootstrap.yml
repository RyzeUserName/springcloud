spring:
  cloud:
    config:
      label: master
      uri: http://localhost:9091
      name: spring-cloud-config   # application-profile-label
      profile: simple
logging:
  level:
    root: debug
