server:
  port: 9090
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/RyzeUserName/springcloud
          search-paths: CONFIG
  application:
    name: config-server